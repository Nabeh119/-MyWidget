import 'package:flutter/material.dart';

class ExTextFormField extends StatelessWidget {
  const ExTextFormField({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.orange,
        title: Text("TextFormField"),
      ),
    );
  }
}
